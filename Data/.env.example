# Пример файла с переменными окружения для RemoteMCP
# Скопируйте этот файл в .env и настройте значения

# Окружение: development, production, test
MCP_ENV=development

# Секретный ключ для подписи токенов (ОБЯЗАТЕЛЬНО изменить в продакшене!)
SECRET_KEY=your-very-secret-key-change-me

# URL сервера (используется в OAuth2 метаданных)
OAUTH2_ISSUER_URL=https://your-domain.com

# Разрешенные источники для CORS (через запятую)
CORS_ORIGINS=https://claude.ai,https://your-app.com

# Уровень логирования: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Настройки сервера
MCP_HOST=0.0.0.0
MCP_PORT=8000

# Время жизни токенов (в секундах)
OAUTH2_TOKEN_EXPIRY=3600
OAUTH2_REFRESH_TOKEN_EXPIRY=2592000
OAUTH2_AUTH_CODE_EXPIRY=600

# Максимальное количество событий на поток
MAX_EVENTS_PER_STREAM=100

# Claude клиент (для продакшена используйте случайный секрет)
CLAUDE_CLIENT_SECRET=claude_secret_key_2024

# Redis URL (для продакшена)
# REDIS_URL=redis://localhost:6379/0

# База данных (для продакшена)
# DATABASE_URL=postgresql://user:password@localhost/mcp_db

# Sentry DSN для мониторинга ошибок (опционально)
# SENTRY_DSN=https://your-sentry-dsn
